version: 1
frontend:
  phases:
    build:
      commands:
        - rm -rf dist
        - mkdir -p dist
        - rsync -a --exclude dist --exclude .git --exclude audit ./ dist/
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths: []
