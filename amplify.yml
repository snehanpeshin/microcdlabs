version: 1
frontend:
  phases:
    build:
      commands:
        - rm -rf dist
        - mkdir -p dist
        - find . -mindepth 1 -maxdepth 1 ! -name dist ! -name .git ! -name audit -exec cp -R {} dist/ \;
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths: []
